This question was also very straight forward nothing much just there was
one segmentation fault
which was resolved when i defined the size of the array

vector<vector<int>> arr(n)

earlier the n was not written here
