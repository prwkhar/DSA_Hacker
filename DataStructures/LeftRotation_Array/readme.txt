This was 20 points question but was actually very easy
temp=arr[0];
    arr.erase(arr.begin());
    arr.push_back(temp);
    d--;
here we just have to use erase function and push the element at the back 
due to cpp methods this was very easy
