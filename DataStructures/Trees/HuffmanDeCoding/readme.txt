Here we only want the leaf nodes we dont
and when we reach the leaf node again intialize current to the root
here a string was encoded by using huffman technique and now we are given the encoded value 
and we have to decode the string from the code 
because of which we are finding till we reach the leaf node and then reassigning the current value
1234